name: tdata
description: Custom type-safe data wrappers 
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0" 

dependencies:
  intl: ^0.20.2
  json_annotation: ^4.9.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.25.15
